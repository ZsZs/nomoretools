server:
  port: 8088

endpoints:
  shutdown:
    sensitive: false
    enabled: true

flyway:
  baselineOnMigrate: true

spring:
  datasource:
    url: jdbc:hsqldb:mem:test_db
    username: SA
